{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <header>
        {% include('inc/nav.html.twig') %}
    </header>

    <body>

    <div style="padding-top: 90px;">
        <a id="h">{{ 'Date du jour :'}}
        {{ "yesterday"|date('D/M/Y') }}</a>
    </div>
    {{ participant }}

    </div>


    </body>
{% endblock %}
